# Codex Handoff

This directory is the local handoff output target for `scripts/codex-next.ps1`.

Generated files such as `current-summary.md`, `next-prompt.md`, `validation.md`,
`codex-output.md`, `*.log`, and `*.json` are local-only and ignored by git.
